[
    {
        "path": "GET /users/:user", // HTTP method and path
        "handler": "require:./user", // Path to a module that exports a route handler function
        "foo": "bar"// Any additional metadata to associate with this route: (optional)
    }
]